import { ModuleShell } from "@/components/ModuleShell";

export default function KeywordsPage() {
  return <ModuleShell title="SEO / Keywords" desc="Research, clustering and intent classification placeholders."
    emptyTitle="No keywords yet" body={["Add seeds in onboarding step 7", "Clusters form after research worker connects"]} />;
}
