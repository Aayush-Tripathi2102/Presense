import { ModuleShell } from "@/components/ModuleShell";

export default function BacklinksPage() {
  return <ModuleShell title="SEO / Backlinks" desc="Monitoring + target publication pipeline."
    emptyTitle="No backlinks tracked" body={["Connect monitor worker", "Add target publications from onboarding"]} />;
}
